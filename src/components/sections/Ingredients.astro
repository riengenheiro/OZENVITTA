---
import { media } from '../../data/media';

const { ingredients, ingredientsCarousel, backgrounds } = media;
const bg = backgrounds.ingredients;
---

<section 
  class="ingredients section" 
  id="ingredientes"
  style={`--bg-desktop: url('${bg.desktop}')`}
>
  <div class="ingredients__overlay"></div>
  
  <div class="container ingredients__container">
    <div class="ingredients__header text-center">
      <span class="section-label">100% NATURAL</span>
      <h2 class="section-title text-white">Suplemento 100% Natural</h2>
      <p class="section-subtitle ingredients__subtitle">Conheça os ingredientes da fórmula</p>
      <p class="ingredients__intro">
        Combinamos os melhores ingredientes naturais, cuidadosamente selecionados para potencializar seus resultados de forma segura e eficaz.
      </p>
    </div>

    <div class="ingredients__grid">
      {ingredients.map((item) => (
        <div class="ingredient-card">
          <img src={item.image} alt={item.name} loading="lazy" />
          <h3>{item.name}</h3>
          <p>{item.description}</p>
        </div>
      ))}
    </div>

    <div class="ingredients-carousel">
      <div class="ingredients-carousel__track">
        {ingredientsCarousel.map((src) => (
          <img src={src} alt="" class="ingredients-carousel__slide" loading="lazy" />
        ))}
      </div>
    </div>

    <div class="text-center ingredients__cta">
      <a href="#oferta" class="btn btn--primary">Quero emagrecer</a>
    </div>
  </div>
</section>

<style>
  .ingredients {
    background-image: var(--bg-desktop);
    background-size: cover;
    background-position: center;
    position: relative;
    min-height: 80vh;
  }

  .ingredients__overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(135deg, rgba(36, 110, 178, 0.9) 0%, rgba(36, 53, 111, 0.95) 100%);
  }

  .ingredients__container { position: relative; z-index: 1; }
  .ingredients__header { margin-bottom: 64px; }
  .ingredients__subtitle { color: white; opacity: 0.9; }

  .ingredients__intro {
    max-width: 700px;
    margin: 24px auto 0;
    color: rgba(255, 255, 255, 0.85);
    font-size: 16px;
    line-height: 1.6;
  }

  .ingredients__grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 20px;
  }

  .ingredient-card {
    background: rgba(255, 255, 255, 0.95);
    border-radius: 16px;
    padding: 24px 16px;
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }

  .ingredient-card:hover {
    transform: translateY(-8px);
    box-shadow: 0 12px 32px rgba(0, 0, 0, 0.2);
  }

  .ingredient-card img {
    width: 64px;
    height: 64px;
    margin: 0 auto 16px;
    object-fit: contain;
  }

  .ingredient-card h3 {
    font-size: 16px;
    font-weight: 700;
    color: var(--color-dark);
    margin-bottom: 8px;
  }

  .ingredient-card p {
    font-size: 13px;
    color: var(--color-text-light);
    line-height: 1.4;
  }

  .ingredients-carousel {
    margin-top: 48px;
    overflow: hidden;
    border-radius: 16px;
  }

  .ingredients-carousel__track {
    display: flex;
    gap: 16px;
    overflow-x: auto;
    scroll-snap-type: x mandatory;
    padding: 8px 0;
    -webkit-overflow-scrolling: touch;
  }

  .ingredients-carousel__slide {
    flex: 0 0 auto;
    height: 120px;
    width: auto;
    scroll-snap-align: start;
    border-radius: 12px;
    object-fit: contain;
    background: rgba(255, 255, 255, 0.1);
    padding: 8px;
  }

  .ingredients__cta { margin-top: 48px; position: relative; z-index: 1; }

  @media (max-width: 992px) {
    .ingredients__grid { grid-template-columns: repeat(3, 1fr); }
  }

  @media (max-width: 768px) {
    .ingredients__grid { display: none; }
    .ingredients-carousel { display: block; }
  }

  @media (min-width: 769px) {
    .ingredients-carousel { display: none; }
  }

  @media (max-width: 480px) {
    .ingredients__grid { grid-template-columns: 1fr; }
  }
</style>
